import ChannelNavigator from '.'

const story = {
  title: 'ChannelNavigator',
  component: ChannelNavigator
}
const ChannelNavigatorBar = () => <ChannelNavigator />

export default story
export { ChannelNavigatorBar }
